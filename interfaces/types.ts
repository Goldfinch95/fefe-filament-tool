export type Color = {
  id: string;
  name: string;
  color: string;
  number: number;
};

export type SellData = {
  id: number;
  product: string;
  status: string;
  client: string;
  date: string;
  number: number;
  amount: number;
  observations: string;
  seller: string;
};